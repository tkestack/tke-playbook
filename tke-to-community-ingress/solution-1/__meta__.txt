title = '方案一：独立 TKE NginxIngress 组件迁移到自建 Ingress-nginx'
description = '通过创建独立的 IngressClass 实现 TKE NginxIngress 到社区版 Nginx Ingress 的安全迁移'
class = "TKE"
tag = ["TKE，Ingress"]
draft = false

title_en = 'Solution 1: Migrate TKE NginxIngress using Independent IngressClass'
description_en = 'Secure migration from TKE NginxIngress to community Nginx Ingress by creating independent IngressClass'
class_en = "TKE"
tag_en = ["TKE，Ingress"]
draft = false
